Create Maven Configuration for Run with command line:
tomcat7:run

Create Maven Configuration for Deploy with command line (To be noted, you need a standalone tomcat running, allow manage-script role):
tomcat7:deploy

